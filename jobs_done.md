# Zpráva o realizaci (Jobs Done) – SFTP Plugin

Dokument rekapituluje realizované úkoly a opravy v pluginu **SFTP/SCP pro Open Salamander**.

---

## 📋 Přehled realizovaných úkolů

| Oblast | Popis řešení | Klíčové soubory |
| **Spouštění souborů (+x) na serveru při stisku Enter** | Implementována detekce spustitelných Unixových souborů podle atributů (`+x` bity). Při stisku **Enter** (nebo 2× kliku) na skriptu či binárce se namísto stahování do Windows soubor přímo spustí na serveru (`cd "dir" && ./"file"`) s aplikací `sudo`/`su` prefixu v asynchronním konzolovém okně. Nespustitelné soubory se nadále stahují do `%TEMP%` a otevírají lokálně. Volba `ExecOnEnter` je plně konfigurovatelná v profilu relace (checkbox na stránce SFTP) a ukládá se do registru. | `src/fs1.cpp`, `src/sftp.cpp`, `src/sftpglue.h`, `src/sftpglue.cpp`, `src/lang/lang*.rc` |
| **Vydání verze v1.1.0 & Release balíček** | Projekt povýšen na verzi **v1.1.0**, vygenerován samostatný distribuční balíček `salamander-sftp-plugin-v1.1.0-x64.zip` obsahující `sftp.spl`, `english.slg`, `czech.slg`, `libcrypto-3-x64.dll`, `libssh2.dll`, `z.dll` a dokumentaci. Proveden sync s origin repozitářem a nahrání aktualizovaného archivu na GitHub release. | `src/versinfo.rh2`, `Makefile.mingw`, `.gitignore`, `jobs_done.md` |
| **Podpora mezerníku (Spacebar) pro výpočet velikosti složek** | Vyřešeno omezení jádra Salamandera, které pro pluginy ignoruje mezerník. Implementován vláknový Windows Message Hook (`WH_GETMESSAGE` via `SetWindowsHookEx`), který detekuje stisk `VK_SPACE` na složkách ve vzdáleném SFTP panelu, zprávu zkonzumuje (`WM_NULL`), provede označení položky (`SelectPanelItem`), spustí bleskový server-side výpočet velikosti složky (`FastDirSize`), zapíše velikost do sloupce panelu a posune focus na další položku. | `src/sftp.cpp`, `src/sftp.h`, `src/fs1.cpp`, `src/fs2.cpp`, `src/precomp.h` |
| **Vlastní SFTP server a sudo/su eskalace** | Implementována podpora pro zadání vlastního příkazu `sftp-server` v profilu (např. `sudo /usr/lib/openssh/sftp-server`, `sudo su -c /usr/lib/openssh/sftp-server` nebo `sudo -u www-data`). Implementována funkce `WrapCommandWithSftpServerPrefix` pro automatické obalení a propagaci prefixu na příkazy spouštěné z příkazové řádky Salamandera pod panely. | `src/sftpconn.cpp`, `src/sftpglue.cpp`, `src/sftpglue.h`, `src/fs1.cpp`, `src/fs2.cpp` |
| **Dokumentace & Release (Fork od Dupl3xx)** | Kompletní aktualizace [README.md](file:///c:/Users/filip/AntigravityProjects/salamander-sftp-plugin/README.md) a [README_CZ.md](file:///c:/Users/filip/AntigravityProjects/salamander-sftp-plugin/README_CZ.md) před vydáním releasu. Jasně uveden původ projektu jako fork z `https://github.com/Dupl3xx/salamander-sftp-plugin`, uvedeno poděkování původnímu autorovi a detailně zdokumentovány všechny klíčové rozdíly a vylepšení (statický build bez MinGW DLL, vlastní příkaz `sftp-server` pro sudo/su eskalaci, asynchronní konzole příkazů, aktivní keepalive pro TrueNAS, bleskový Calc Size přes mezerník i menu, oprava focusu při návratu do `..`). | `README.md`, `README_CZ.md`, `jobs_done.md`, `implementation_plan.md` |
| **Aktivní Keepalive & TrueNAS podpora** | Vyřešeno odpojování od TrueNAS / OpenSSH serverů způsobené nečinností v UI a `ClientAliveInterval`. Implementován periodický FS časovač (`AddPluginFSTimer` každých 8 s), neblokující `SendKeepalive()`, přepnuto na `want_reply = 0` u SSH keepalive sond (zamezení `SSH_MSG_REQUEST_FAILURE`), a přidán transparentní auto-reconnect & retry při čtení složek v `ListCurrentPath`. | `src/sftpconn.cpp`, `src/sftpconn.h`, `src/fs2.cpp` |
| **Vylepšený výpočet velikosti složek (`Calc Size`) & Klávesové zkratky** | Přepracován výpočet velikosti na serveru: přidána rychlá server-side kalkulace `FastDirSize` přes SSH exec (`du -sb` / `du -sk` / `find`) s plnou propagací nakonfigurované `sudo`/`su` elevace (`WrapCommandWithSftpServerPrefix`), dedikovaný a nezamrzající dialog průběhu (`CCalcSizeProgressDlg` / `IDD_CALCSIZEDLG`) se správným titulkem *Výpočet velikosti* a popiskem *Složka:*, možnost okamžitého zrušení (**Storno** bez potvrzovacího dotazu), ochrana proti zacyklení na symlincích a automatický update velikostí v panelu Salamandera. Povolena a namapována standardní klávesová zkratka **`Ctrl+Shift+F10`**, mezerník (**Spacebar**) a položky v kontextovém menu. | `src/sftpconn.cpp`, `src/sftpconn.h`, `src/fs2.cpp`, `src/sftp.cpp`, `src/sftp.h`, `src/lang/lang*.rc` |
| **Udržování spojení & Auto-reconnect** | Zavedení TCP keepalive sond (15s idle / 5s interval) a SSH keepalive pakety. Implementace přesné detekce živosti socketu v `IsConnected()` (`select` + `MSG_PEEK`) a transparentní reconnect v `SftpEnsureConnected`. | `src/sftpconn.cpp`, `src/sftpconn.h`, `src/sftpglue.cpp` |
| **Navigace & Focus na `..`** | Při opuštění složky přes `..` (`isDir == 2`) se extrahuje název opouštěné podsložky a předá se jako `suggestedFocusName` do `ChangePanelPathToPluginFS`. Salamander tak správně vyfokusuje složku v nadřazeném výpisu. | `src/fs1.cpp` |
| **Protokoly SFTP & SCP** | Plná podpora SFTP v3 a SCP přenosů přes libssh2 + OpenSSL. | `src/sftpconn.cpp`, `src/fs2.cpp` |
| **Přihlašovací dialog** | Správa uložených relací, profily připojení, šifrování hesel. | `src/fs1.cpp`, `src/sftpglue.cpp` |
| **Statické linkování & Cppcheck audit** | Přidání `-static` do `Makefile.mingw` (odstranění dynamické závislosti na `libwinpthread-1.dll`), statické slinkování pthreads + libgcc + libstdc++ + libssh2. Provedení statické analýzy přes Cppcheck 2.21 a vyřešení nálezů. | `Makefile.mingw`, `src/sftpconn.cpp`, `src/dialogs.cpp` |
| **Příkazová konzole** | Spouštění příkazů na serveru s reálným výstupem v okně konzole. | `src/dialogs.cpp`, `src/menu.cpp` |
